Pour utiliser ce programme, il faut Qt5.
Tout d'abord, il faut compiler avec la commande: <span class="code">qmake</span>

Et ensuite, il faut compiler le code avec la commande: <span class="code">make</span>

Enfin, on exécute le code avec la commande: <span class="code">./pong</span>
